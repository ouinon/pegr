<IfModule mod_rewrite.c>
    
    RewriteEngine on

    RewriteRule ^node/(.+) http://local.pegs.website:8090/$1 [P,L]
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule . index.html [L]
    # Redirect a whole subdirectory:
    
</IfModule>